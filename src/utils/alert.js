import { Alert } from "reactstrap";
const alert = () => {
    return ( 
        <Alert color="primary">
        This is a primary alert — check it out!
      </Alert>
     );
}
 
export default alert;